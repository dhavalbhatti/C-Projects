#include <stdio.h>

int main (){
    int i, arr[5]={1,2,3,4,5};
   for (i = 0; i < 5; i++)
   {
    printf("%d \n", arr[i]);
   }
   
    return 0;
}

/*
array index starts from = 0
if we exceed the array value, it will show 0 only as the output
*/